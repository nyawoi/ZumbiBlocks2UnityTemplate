using UnityEngine;

public class HitDetector
{
	public static DamageTaker Test(Vector3 origin, Vector3 forward, Vector3 right, float range, float distance, int layerMask, out Vector3 hitpos, float radius = 0f)
	{
		hitpos = default(Vector3);
		return null;
	}

	public static DamageTaker Cast(Vector3 origin, Vector3 vector, float radius, float distance, int layerMask, out Vector3 hitpos)
	{
		hitpos = default(Vector3);
		return null;
	}
}
